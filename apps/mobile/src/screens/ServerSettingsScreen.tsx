// Re-export the shared ServerSettings component as ServerSettingsScreen
// for backwards compatibility with mobile navigation.
export { ServerSettings as ServerSettingsScreen } from '@ants/ui';
export type { ServerSettingsProps as ServerSettingsScreenProps } from '@ants/ui';
