# Open the drawer and close it with the X button.
appId: host.exp.Exponent
---
- launchApp
- assertVisible:
    id: "openmgr-home-screen"

# Open the drawer
- tapOn:
    id: "openmgr-drawer-toggle"
- assertVisible:
    id: "openmgr-drawer"

# Close the drawer via X button
- tapOn:
    id: "openmgr-drawer-close"

# Verify we're back on the home screen (drawer dismissed)
- assertVisible:
    id: "openmgr-home-screen"
