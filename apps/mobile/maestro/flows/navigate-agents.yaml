# Navigate to Agents via the drawer menu.
appId: host.exp.Exponent
---
- launchApp
- assertVisible:
    id: "ants-home-screen"

# Open the drawer
- tapOn:
    id: "ants-drawer-toggle"
- assertVisible:
    id: "ants-drawer"

# Tap Agents in the drawer
- tapOn:
    id: "ants-drawer-agents"

# Verify we're on the agents screen
- assertVisible:
    id: "ants-screen-agents"
