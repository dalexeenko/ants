// AUTO-GENERATED by @ants/test-scenarios — do not edit manually
// Source: scenarios/create-session.yaml

import { test, expect, type ElectronApplication, type Page } from '@playwright/test';
import { launchApp, closeApp } from '../electron';

let app: ElectronApplication;
let page: Page;

test.beforeAll(async () => {
  ({ app, page } = await launchApp());
});

test.afterAll(async () => {
  await closeApp(app);
});

test.describe('Create a new session', () => {
  test('Create a new chat session within an existing project', async () => {
    // Skip when no project exists (clean CI environment)
    const welcomeScreen = page.getByTestId('ants-welcome-screen');
    const hasWelcome = await welcomeScreen.isVisible().catch(() => false);
    test.skip(hasWelcome, 'Requires an existing project — welcome screen is showing');
    await page.getByTestId('ants-project-new-session').click();
    await expect(page.getByTestId('ants-chat-input')).toBeVisible();
    await expect(page.getByTestId('ants-chat-view')).toBeVisible();
  });
});
