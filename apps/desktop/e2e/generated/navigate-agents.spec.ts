// AUTO-GENERATED by @openmgr/test-scenarios — do not edit manually
// Source: scenarios/navigate-agents.yaml

import { test, expect, type ElectronApplication, type Page } from '@playwright/test';
import { launchApp, closeApp } from '../electron';

let app: ElectronApplication;
let page: Page;

test.beforeAll(async () => {
  ({ app, page } = await launchApp());
});

test.afterAll(async () => {
  await closeApp(app);
});

test.describe('Navigate to Agents', () => {
  test('Navigate to the agents screen from the initial state', async () => {
    await page.getByTestId('openmgr-icon-rail-agents').click();
    await expect(page.getByTestId('openmgr-agents-panel')).toBeVisible();
  });
});
