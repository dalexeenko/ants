// AUTO-GENERATED by @ants/test-scenarios — do not edit manually
// Source: scenarios/navigate-settings.yaml

import { test, expect, type ElectronApplication, type Page } from '@playwright/test';
import { launchApp, closeApp } from '../electron';

let app: ElectronApplication;
let page: Page;

test.beforeAll(async () => {
  ({ app, page } = await launchApp());
});

test.afterAll(async () => {
  await closeApp(app);
});

test.describe('Navigate to Settings', () => {
  test('Navigate to the settings screen from the initial state', async () => {
    await page.getByTestId('ants-icon-rail-settings').click();
    await expect(page.getByTestId('ants-settings-panel')).toBeVisible();
  });
});
