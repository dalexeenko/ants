// AUTO-GENERATED by @ants/test-scenarios — do not edit manually
// Source: scenarios/launch.yaml

import { test, expect, type ElectronApplication, type Page } from '@playwright/test';
import { launchApp, closeApp } from '../electron';

let app: ElectronApplication;
let page: Page;

test.beforeAll(async () => {
  ({ app, page } = await launchApp());
});

test.afterAll(async () => {
  await closeApp(app);
});

test.describe('App Launch', () => {
  test('Verify the app launches and shows its initial state', async () => {
    await expect(page.getByTestId('ants-app')).toBeVisible();
    await expect(page.getByTestId('ants-icon-rail')).toBeVisible();
    await expect(page.getByTestId('ants-welcome-screen')).toBeVisible();
  });
});
