{
  "name": "@ants/agent-task-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Episode harness for RL with verifiable rewards",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@ants/agent-verifiers": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.0.0"
  }
}
