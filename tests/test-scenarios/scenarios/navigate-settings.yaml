name: Navigate to Settings
description: Navigate to the settings screen from the initial state
preconditions: []
steps:
  # Desktop: click icon rail settings button
  - tap:
      testId: ants-icon-rail-settings
      desktop: true
  - assertVisible:
      testId: ants-settings-panel
      desktop: true

  # Mobile: open drawer, tap settings
  - tap:
      testId: ants-drawer-toggle
      mobile: true
  - tap:
      testId: ants-drawer-settings
      mobile: true
  - assertVisible:
      testId: ants-settings-screen
      mobile: true
